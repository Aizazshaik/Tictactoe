package TicTacToe.Models;

public enum Cellstate {
    EMPTY,
    FILLED,
    BLOCKED
}
//BLOCKED just in case if we are using different version of game
//where certain cells are not accessible by any player